name: Deploy docs

on:
  push:

jobs:
  deploy-docs:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print My Name
        uses: sstk-actions/runner-assume-role@v6.1.0
